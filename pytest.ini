[pytest]
testpaths = app
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = app
addopts =
    -q
    --strict-markers
    --tb=short
filterwarnings =
    ignore::DeprecationWarning
