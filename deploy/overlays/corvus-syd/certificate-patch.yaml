apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: corvus-tls
spec:
  dnsNames:
    - secretserver.sigaint.au
  issuerRef:
    name: le-production
    kind: ClusterIssuer
    group: cert-manager.io
