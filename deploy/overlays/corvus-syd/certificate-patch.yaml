apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: corvus-tls
spec:
  dnsNames:
    - corvus.sigaint.au
  issuerRef:
    name: le-production
    kind: ClusterIssuer
    group: cert-manager.io
