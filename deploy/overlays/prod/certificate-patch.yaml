apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: secretserver-tls
spec:
  dnsNames:
    - secretserver.example.com
    - secretserver.example.internal
  issuerRef:
    name: letsencrypt-prod
