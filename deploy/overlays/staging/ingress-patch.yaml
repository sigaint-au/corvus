apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: secretserver
  annotations:
    external-dns.alpha.kubernetes.io/hostname: "secretserver.staging.internal"
spec:
  tls:
    - hosts:
        - secretserver.staging.internal
      secretName: secretserver-tls
  rules:
    - host: secretserver.staging.internal
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: secretserver-app
                port:
                  name: http
