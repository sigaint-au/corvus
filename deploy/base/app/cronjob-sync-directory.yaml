apiVersion: batch/v1
kind: CronJob
metadata:
  name: corvus-sync-directory
  labels:
    app.kubernetes.io/name: app
    app.kubernetes.io/component: job
    app.kubernetes.io/part-of: corvus
spec:
  schedule: "30 2 * * *"
  timeZone: Etc/UTC
  concurrencyPolicy: Forbid
  startingDeadlineSeconds: 900
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      activeDeadlineSeconds: 900
      template:
        metadata:
          labels:
            app.kubernetes.io/name: app
            app.kubernetes.io/component: job
            app.kubernetes.io/part-of: corvus
        spec:
          serviceAccountName: corvus-app
          automountServiceAccountToken: false
          restartPolicy: OnFailure
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            fsGroup: 10001
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: sync-directory
              image: quay.io/sigaint/corvus:latest
              imagePullPolicy: IfNotPresent
              command: ["flask", "--app", "app", "sync-directory", "--source", "ldap"]
              envFrom:
                - configMapRef:
                    name: corvus-app-env
              env:
                - name: DATABASE_URL
                  valueFrom:
                    secretKeyRef:
                      name: corvus-app-secrets
                      key: DATABASE_URL
                - name: DATABASE_ADMIN_URL
                  valueFrom:
                    secretKeyRef:
                      name: corvus-app-secrets
                      key: DATABASE_ADMIN_URL
                - name: JWT_SECRET
                  valueFrom:
                    secretKeyRef:
                      name: corvus-app-secrets
                      key: JWT_SECRET
                - name: MASTER_KEY
                  valueFrom:
                    secretKeyRef:
                      name: corvus-app-secrets
                      key: MASTER_KEY
                - name: SECRET_KEY
                  valueFrom:
                    secretKeyRef:
                      name: corvus-app-secrets
                      key: SECRET_KEY
              resources:
                requests:
                  cpu: 50m
                  memory: 128Mi
                limits:
                  cpu: 500m
                  memory: 256Mi
              securityContext:
                allowPrivilegeEscalation: false
                privileged: false
                readOnlyRootFilesystem: true
                runAsNonRoot: true
                runAsUser: 10001
                capabilities:
                  drop: ["ALL"]
                seccompProfile:
                  type: RuntimeDefault
              volumeMounts:
                - name: tmp
                  mountPath: /tmp
                - name: home
                  mountPath: /home/appuser
          volumes:
            - name: tmp
              emptyDir:
                medium: Memory
                sizeLimit: 64Mi
            - name: home
              emptyDir:
                medium: Memory
                sizeLimit: 16Mi
